apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orgId: 1
    url: https://prometheus:9090/admin/monitor/prometheus/
    jsonData:
      tlsAuth: true
      tlsSkipVerify: true
    basicAuth: false
    isDefault: true
    editable: true